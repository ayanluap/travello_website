import React from 'react';
import Logo from '../layout/Logo';
import '../../styles/Header.css';

const Header = () => {
  return (
    <div id="header">
      <Logo />
    </div>
  );
};

export default Header;
